---
version: 1
slug: "frontend"
primary_target: "frontend"
related_targets: []
---

## Direction contract

**THESIS** — Seattle street-safety evidence behaves like a calibrated civic instrument: the map is the working field, the selected neighborhood is the active channel, and every supporting panel exists to explain that channel. It refuses the category-default stack of friendly rounded KPI cards floating above a generic map.

**OWN-WORLD** — A midnight-navy operations field is ruled by blue-gray hairlines, square modules, electric-blue selection, cyan analytical support, amber incomplete-period signals, and fatality red used only for genuine fatal outcomes. Condensed uppercase display labels, restrained sans body copy, and tabular numerics create the character of a transit control room without borrowing decorative machinery.

**STORY** — A planner first sees the whole city and the selected time window, chooses a neighborhood from the map or searchable selector, then reads burden, injury outcomes, situational shares, and complete-year trend evidence in one synchronized path. Citywide Analysis is a separate analytical station whose severity filters never leak into neighborhood evidence.

**FIRST VIEWPORT** — Desktop uses a 72 px navigation rail, a thin command strip with route and persistent year controls, a map occupying roughly two-thirds of the working field, and a 360 px neighborhood inspector on the right. The active polygon emits a precise electric-blue edge and the inspector begins with one headline burden value, not a marketing hero. Mobile collapses to compact top navigation, selector, map, then a squared contextual sheet. The signature interaction is a single selection pulse that travels from polygon edge to inspector rule and trend marker, disabled under reduced motion.

**FORM** — Civic Safety Operations Console, the user-pinned direction. It supersedes the direction roll’s sixth grounded candidate; the roll’s fabric-like transformation discipline is retained only as interaction logic—one controlled selection pulls the whole evidence surface into alignment. The implementation became code-led after the Figma Starter-plan MCP quota stopped screen-frame creation; the Figma foundations/component library and user-supplied images remain critique references rather than pixel specifications. Seed key: `a07566db`.

**FINISH** — unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
